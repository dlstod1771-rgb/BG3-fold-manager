# BG3 Mod Bridge

발더스 게이트 3 모드 안내 글을 링크별 컬렉션으로 보관하고, 다운로드 파일과 적용 상태를 Vortex 및 BG3 `Mods` 폴더 사이에서 관리하는 Windows 프로그램입니다.

## 시작

1. 다른 사용자의 안내 페이지 주소를 넣고 **수집**을 누릅니다.
2. 항목을 선택하고 **링크 열기**로 필요한 모드 페이지를 확인합니다.
3. Nexus/Patreon 항목은 **링크 열기** 또는 **다운로드 대기**를 누른 뒤 Brave에서 다운로드 버튼을 한 번 누릅니다. 프로그램이 새 파일을 감지해 해당 행에 연결합니다.
4. 체크박스를 끄면 BG3/Vortex에서 해제하고 상태를 목록에 표시합니다.

**공개 링크 전체 받기**는 Google Drive·직접 링크만 순서대로 받습니다. Nexus/Patreon은 사이트의 다운로드 버튼을 프로그램이 누르지 않습니다.

목록 위 필터에서 검색어·출처·적용 여부·다운로드 유무를 조합할 수 있습니다. `적용`, `모드/링크`, `출처`, `다운로드`, `선택 그룹`, `상태` 열 제목을 누르면 오름차순/내림차순으로 정렬됩니다. 하단 설명의 파란 URL은 클릭 가능한 링크입니다.

## 적용과 해제

- `.pak`: 첫 열을 더블클릭하거나 **☑ 적용/해제**를 누르면 BG3 `Mods` 폴더에 복사/삭제됩니다. 외부에서 파일 내용이 바뀌었으면 안전을 위해 삭제하지 않습니다.
- `.zip`, `.rar`, `.7z`: 적용하면 Vortex의 공식 `--install-archive` 기능으로 전달됩니다. Vortex 설치 화면을 완료하세요.
- 압축 모드 해제: 먼저 프로그램의 **Vortex 연동 설치**를 한 번 누르고 Vortex를 완전히 재시작하세요. 이후 체크박스가 Vortex 공식 API로 `Enable/Disable`하며 자동 배치합니다. 연동을 설치하지 않은 상태에서는 Vortex 화면에서 직접 `Disable`합니다.
- **선택그룹 지정**: AstarionF/AstarionF2/AstarionF3처럼 동시에 하나만 써야 할 행들을 함께 선택해 같은 그룹명을 지정합니다. 하나를 적용하면 같은 그룹의 다른 단일 `.pak`는 자동 해제됩니다.

## 보관 위치와 동기화

- 기본 컬렉션: `%USERPROFILE%\Documents\BG3 Mod Bridge`
- 기본 BG3 Mods: `%LOCALAPPDATA%\Larian Studios\Baldur's Gate 3\Mods`
- 각 컬렉션에는 `collection.json`, `설치안내.txt`, `downloads` 하위 폴더가 생깁니다.
- 컬렉션 이름은 `내용, 출처사이트-작성자` 형식입니다. 예: `Npc 외형 변경 모드 14 (아스타리온 여체화),발게3 디시-dragon`
- 경로가 다르면 상단 **설정**에서 바꿀 수 있습니다.
- Vortex에서 설치/해제한 결과는 BG3 `Mods` 및 Vortex staging 폴더를 읽어 표시합니다. Vortex 내부 데이터베이스는 직접 수정하지 않습니다.
- `Vortex 연동 설치`는 `%APPDATA%\Vortex\plugins\bg3-mod-bridge`에 두 개의 작은 확장 파일을 복사합니다. 제거하려면 Vortex 종료 후 그 폴더만 삭제하면 됩니다.
- Nexus/Patreon은 Brave의 기본 다운로드 폴더를 감시합니다. 한 번에 한 항목만 다운로드 대기로 두면 새 모드 파일이 선택한 행에 자동 연결됩니다.
- 일반 링크와 하단 하이퍼링크도 Brave로 엽니다.

## 주의

- 안내 글에 `Baldurs Gate 3\Data` 등 별도 설치 경로가 명시된 특수 모드는 자동 배치하지 않습니다. 설명 칸을 확인하고 해당 제작자의 안내를 따르세요.
- 게임과 Vortex에서 모드 배치 작업을 하는 동안에는 게임을 종료해 두는 편이 안전합니다.
- 컬렉션 삭제는 보관 사본만 지우며 이미 적용된 BG3/Vortex 파일은 건드리지 않습니다.
- 실행에 실패하면 CMD 창이 닫히지 않고 오류를 표시하며, 자세한 기록은 `%LOCALAPPDATA%\BG3ModBridge\error.log`에 저장됩니다.
